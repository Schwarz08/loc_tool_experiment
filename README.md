# loc_tool_experiment
 Location-based planning is using existing data to plan location-based cell parameters (ex: LTE TAC). This tool runs simulations to find the optimal parameters to be used for location-based planning.
